import scala.utils._
import scala.io.StdIn._

object Fibonacci{
 
 def calc_fib(n: Int): Int = {
  
 }

 
 
 def main(args: Seq[String]){
     val n = readInt()
     println(calc_fib(n))
 }
}